# Optional local overlay for manual Hydra composition.
# Note: `sdpo_swe.yaml` and `rft_swe.yaml` do NOT include this file in defaults.
# Keep values here only if you explicitly compose `+user` yourself.

vars:
  dir: /home/${oc.env:USER}/small-swe-train       # project root
  log_dir: /home/${oc.env:USER}/logs/small-swe     # W&B / tensorboard logs
  ckpt_dir: /home/${oc.env:USER}/checkpoints/small-swe
